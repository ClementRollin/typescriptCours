import React from "react";
import RandomChoice from "./components/RandomChoice";

const App: React.FC = () => {
    return (
        <div>
            <RandomChoice />
        </div>
    );
};

export default App;